run.config:
  engine: nodejs
  engine.config:
    runtime: nodejs-8.6

data.db:
  image: nanobox/mongodb:3.4
  config:
    objcheck: true
    log_verbosity: 'v'

web.main:
  start:
    node: npm run build && node dist/main.js
